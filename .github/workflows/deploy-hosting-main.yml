name: Deploy Hosting Main

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to deploy
        required: false
        default: main

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref_name }}

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter pub get
        run: flutter pub get

      - name: Set up Firebase credentials
        env:
          FIREBASE_SERVICE_ACCOUNT_ROBULINGO_635C5: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ROBULINGO_635C5 }}
        run: |
          if [ -z "$FIREBASE_SERVICE_ACCOUNT_ROBULINGO_635C5" ]; then
            echo "Missing secret: FIREBASE_SERVICE_ACCOUNT_ROBULINGO_635C5"
            exit 1
          fi
          echo "$FIREBASE_SERVICE_ACCOUNT_ROBULINGO_635C5" > "$RUNNER_TEMP/firebase-key.json"
          echo "GOOGLE_APPLICATION_CREDENTIALS=$RUNNER_TEMP/firebase-key.json" >> "$GITHUB_ENV"

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Deploy Robulingo and DailyWords
        run: |
          export SOURCE_REF="$GITHUB_SHA"
          tools/deploy_firebase_hosting.sh prod
